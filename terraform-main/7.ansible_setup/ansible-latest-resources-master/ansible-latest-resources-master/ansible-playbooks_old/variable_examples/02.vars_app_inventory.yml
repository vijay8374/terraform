### Declare a variable  at Appserver host inventory file and print the value.

- name: Print a variable from play
  hosts: app
  # vars:
  #  URL: play.google.com
  tasks:
    - name: Print a variable
      debug:
        msg: "URL = {{URL}}"
