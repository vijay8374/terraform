### Declare a variable  at inventory file and print the value.

- name: Print a variable from play
  hosts: webserver
  # vars:
  #  URL: play.google.com
  tasks:
    - name: Print a variable
      debug:
        msg: "URL = {{URL}}"
